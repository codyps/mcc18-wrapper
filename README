Wraps up the mcc18 compiler binaries so that they preform better in makefiles and other build systems.

- Uses wine if necissary
- Exit codes returned correctly
- Junk output discarded
- Convert line endings
